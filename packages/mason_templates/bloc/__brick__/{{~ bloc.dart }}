import 'package:bloc/bloc.dart';
import 'package:freezed_annotation/freezed_annotation.dart';
import 'package:injectable/injectable.dart';
import 'package:urflutter_core/core/state/async_state.dart';
import 'package:urflutter_core/core/logging/logging.dart';

part '{{name.snakeCase()}}_bloc.freezed.dart';
part '{{name.snakeCase()}}_event.dart';
part '{{name.snakeCase()}}_state.dart';

@injectable
class {{name.pascalCase()}}Bloc extends Bloc<{{name.pascalCase()}}Event, {{name.pascalCase()}}State> {
  {{name.pascalCase()}}Bloc() : super(const {{name.pascalCase()}}State()) {
    on<_Initialized>((event, emit) async {
      emit(state.copyWith(initialized: const AsyncState.loading()));

      try {
        emit(
          state.copyWith(
            initialized:  AsyncState.done,
          ),
        );
      } catch (e) {
        AppLogger.e('Error initialized setting', e.toString());
        
        emit(state.copyWith(initialized: e.toAsyncError()));
      }
    });
  }
}

